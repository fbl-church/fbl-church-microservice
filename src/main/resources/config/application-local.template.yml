# Template for setting configurations for local development
server:
  port: 8000

spring:
  datasource:
    username:
    password:
    url:

ftp:
  server: ftp-nas.ddnsfree.com
  port: 21
  environment: DEV
  username:
  password:

jwt:
  secret: "local-key"
email:
  sendgrid-key:
