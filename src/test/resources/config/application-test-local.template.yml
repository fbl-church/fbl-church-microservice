spring:
  datasource:
    username:
    password:
    url: jdbc:mysql://localhost:3306

security:
  allowed:
    domains: "*"
  signing-key: "test-local-key"

ftp:
  server: fake-ftp-url
  port: 21
  environment: DEV
  username: test
  password: password
